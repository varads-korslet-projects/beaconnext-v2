name: Auto Pull on Commit

on:
  push:
    branches:
      - development

jobs:
  auto_pull:
    runs-on: self-hosted

    - name: Pull changes
      run: git pull

    - name: Restart pm2
      run: pm2 restart all